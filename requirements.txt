fastapi>=0.115.0
uvicorn[standard]>=0.30.0
requests>=2.31.0
python-dotenv>=1.0.0

# LLM + Vector DB (lightweight — no PyTorch, no LlamaIndex)
groq>=0.9.0
pinecone-client>=5.0.0

# Agentic RAG - web search fallback
duckduckgo-search>=7.0.0
